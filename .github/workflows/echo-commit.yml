name: Echo commit

on:
  push:

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Echo Commit Hash
        run: |
          echo $(git rev-parse --short HEAD)
